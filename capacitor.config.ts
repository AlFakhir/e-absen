
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alfakhir.absensipintar',
  appName: 'E-Absensi Pintar',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
